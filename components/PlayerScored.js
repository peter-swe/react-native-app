import {StyleSheet, Text, View, TouchableOpacity} from "react-native";
import React from "react";

const PlayerScored = () => {
  return <TouchableOpacity></TouchableOpacity>;
};

export default PlayerScored;

const styles = StyleSheet.create({});
